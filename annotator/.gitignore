node_modules/
.next/
next-env.d.ts
.env.local
.env*.local
.vercel
*.tsbuildinfo

# content/ is built from data/annotator/ by scripts/sync-content.mjs and is kept
# out of git by the root .gitignore (annotator/content/). It is deliberately NOT
# listed here: `vercel` deploys run from this directory and skip anything a
# local .gitignore excludes, and the corpus must be uploaded.

# Local file-backed annotation store (dev only)
.dev-data/
.env*
